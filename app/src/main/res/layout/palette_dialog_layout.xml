<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/palette_dialog_constraint_layout"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Palette Dialog Icon"
        android:id="@+id/palette_dialog_title_icon"
        app:layout_constraintTop_toTopOf="@id/palette_dialog_constraint_layout"
        app:layout_constraintStart_toStartOf="@id/palette_dialog_constraint_layout"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"
        android:src="@drawable/ic_color_palette"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/palette_dialog_title_text_view"
        android:text="Select Theme Color Scheme:"
        app:layout_constraintTop_toTopOf="@id/palette_dialog_constraint_layout"
        app:layout_constraintStart_toEndOf="@id/palette_dialog_title_icon"
        android:textSize="20dp"
        android:padding="10dp"
        android:textAlignment="center"
        />

    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintTop_toBottomOf="@id/palette_dialog_title_text_view"
        app:layout_constraintBottom_toTopOf="@id/palette_button_linear_layout"
        android:id="@+id/palette_dialog_radio_group">
    </RadioGroup>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/palette_button_linear_layout"
        app:layout_constraintTop_toBottomOf="@id/palette_dialog_radio_group"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:orientation="horizontal"
        >

        <android.support.v7.widget.AppCompatButton
            android:id="@+id/palette_dialog_cancel_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@android:string/cancel"
            android:textAlignment="center"
            android:layout_weight="1"
            />

        <android.support.v7.widget.AppCompatButton
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/palette_dialog_ok_button"
            android:text="@android:string/ok"
            android:textAlignment="center"
            android:layout_weight="1"
            />

    </LinearLayout>
</android.support.constraint.ConstraintLayout>